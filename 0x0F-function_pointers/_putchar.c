#include "main.h"
#include <stdio.h>

/**
 * _putchar - writes character c to stdout
 * @c: the character to print
 *
 * Return: on success 1 or else -1
 */
int _putchar(char c)
{
	return (write(1, &c, 1);
}
